#include <stdio.h>
#include <dirent.h>
#include "romSet.h"
#include "romVersion.h"

// Reads and lays out the structure of the ROM set.
RomSet readRomSet(DIR* romSetDir) {
	RomSet romSet;
	return romSet;
}
